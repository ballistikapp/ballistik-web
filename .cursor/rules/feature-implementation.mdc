---
description: "Feature implementation guidelines and patterns"
alwaysApply: true
---

# Feature Implementation Guidelines

## Documentation-First Workflow (CRITICAL)

Before writing any code:

1. **Read existing docs** in `docs/implementation/` related to the feature
2. **Update documentation** with planned changes (data models, flows, endpoints)
3. **Get clarification** if requirements are unclear
4. **Then implement** following the documented design

## Implementation Process

1. **Analyze the feature requirements**:

   - Understand the business logic and user flows
   - Identify database operations and data models
   - Note API endpoints and their behavior
   - Document findings in `docs/implementation/`

2. **Design the implementation**:

   - Create/update Prisma schema if needed
   - Define Zod schemas for validation
   - Plan service layer functions
   - Design tRPC router endpoints
   - Update implementation docs with design decisions

3. **Implement in layers**:

   - Start with Prisma schema (if new tables needed)
   - Add Zod schemas
   - Implement service functions
   - Create tRPC router
   - Build UI components
   - Update docs with any implementation changes

4. **Maintain quality standards**:

   - Comprehensive error handling
   - Type safety throughout
   - Clear separation of concerns
   - Robust validation
   - Keep documentation current

## Common Patterns

### Creating a New Feature

1. **Schema** (`server/schemas/[feature].schema.ts`):

```typescript
import { z } from "zod";

export const createFeatureSchema = z.object({
  name: z.string().min(1),
});

export type CreateFeatureInput = z.infer<typeof createFeatureSchema>;
```

2. **Service** (`server/services/[feature].service.ts`):

```typescript
import type { CreateFeatureInput } from "@/server/schemas";
import { prisma } from "@/lib/generated/prisma";

export const featureService = {
  async create(input: CreateFeatureInput) {
    return await prisma.feature.create({ data: input });
  },
};
```

3. **Router** (`server/trpc/routers/[feature].router.ts`):

```typescript
import { router, publicProcedure } from "../trpc";
import { createFeatureSchema } from "@/server/schemas";
import { featureService } from "@/server/services";

export const featureRouter = router({
  create: publicProcedure
    .input(createFeatureSchema)
    .mutation(async ({ input }) => {
      return await featureService.create(input);
    }),
});
```

4. **Add to app router** (`server/trpc/routers/_app.ts`):

```typescript
export const appRouter = router({
  // ... existing routers
  feature: featureRouter,
});
```
